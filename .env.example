# URL of the deployed Cloudflare Worker (or any compatible server) that
# hosts the WebSocket /ws/:code endpoint and the /new HTTP endpoint.
# Leave unset to default to http://localhost:8787 during local dev.
VITE_SERVER_URL=https://three-musketeers-server.example.workers.dev
